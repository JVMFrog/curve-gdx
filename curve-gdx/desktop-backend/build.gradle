publishing {
    publications {
        desktopBackend(MavenPublication) {
            from components.java

            groupId = "$groupID"
            artifactId = name
            version = rootProject.version
        }
    }
}

dependencies {
    implementation project(':curve-gdx:core')
}