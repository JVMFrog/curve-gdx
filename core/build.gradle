publishing {
    publications {
        core(MavenPublication) {
            from components.java

            groupId = "$groupID"
            artifactId = name
            version = rootProject.version
        }
    }
}